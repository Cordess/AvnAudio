{
  "name": "AvnAudio",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet build AvnAudio/AvnAudio.csproj",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet test AvnAudio/AvnAudio.csproj",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
